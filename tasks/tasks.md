# Tasks

1. Fill secrets in backend/.env
2. Run `python backend/ingest.py` to index docs
3. Start backend: `uvicorn backend.main:app --reload --port 8000`
4. Start frontend: `cd book && npm install && npm start`
5. Verify chat flow
